// Prisma 7 configuration — database connection via adapter.
// See: https://pris.ly/d/config-datasource
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
});
